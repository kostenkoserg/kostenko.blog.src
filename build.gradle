plugins {
  id 'base'
}

defaultTasks 'bake'

task bake_ru(type: GradleBuild) {
  buildFile = 'build_ru.gradle'
  tasks = ['bake']
}

task bake_en(type: GradleBuild) {
  buildFile = 'build_en.gradle'
  tasks = ['bake']
}

task bake() {
  dependsOn bake_ru
  dependsOn bake_en
}
